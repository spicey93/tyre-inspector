<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Tyre Inspector</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .hero { padding: 2.5rem 0; }
    .code-input { text-transform: uppercase; letter-spacing: .08em; }
    .muted { color: #6c757d; }
  </style>
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm">
    <div class="container">
      <a class="navbar-brand fw-semibold" href="/">Tyre Inspector</a>
      <div class="ms-auto">
        <a class="btn btn-primary btn-sm" href="/inspections/new">Create Inspection</a>
      </div>
    </div>
  </nav>

  <main class="container hero">
    <!-- Optional server messages -->
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <% if (typeof message !== 'undefined' && message) { %>
      <div class="alert alert-success"><%= message %></div>
    <% } %>

    <!-- Align to top so the right column doesn't 'float' -->
    <div class="row g-4 align-items-start">
      <div class="col-12 col-lg-7">
        <h1 class="display-6 fw-semibold mb-2">Create. Share. View.</h1>
        <p class="lead text-secondary mb-4">
          No account needed. Create a tyre inspection, get a unique code, and share it with your customer.
        </p>

        <div class="card shadow-sm">
          <div class="card-body">
            <h2 class="h5 mb-3">View an Inspection</h2>
            <form action="/inspections" method="get" novalidate>
              <label for="code" class="form-label">Enter Code</label>
              <div class="input-group">
                <input
                  id="code"
                  name="code"
                  type="text"
                  class="form-control code-input"
                  placeholder="e.g. 9F3A2C81"
                  pattern="[A-Fa-f0-9]{8}"
                  maxlength="8"
                  required
                >
                <button class="btn btn-primary" type="submit">Go</button>
              </div>
              <div class="form-text">8-character code (hex). Not case sensitive.</div>
            </form>
          </div>
        </div>
      </div>

      <div class="col-12 col-lg-5">
        <div class="card border-0 shadow-sm">
          <div class="card-body">
            <h3 class="h6 text-uppercase text-muted mb-3">How it works</h3>
            <ol class="mb-0">
              <li class="mb-2"><strong>Create</strong> an inspection with vehicle details and tyres.</li>
              <li class="mb-2"><strong>Get a code</strong> (e.g., <code>9F3A2C81</code>) after saving.</li>
              <li class="mb-0"><strong>Share the code</strong> so anyone can view the inspection.</li>
            </ol>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="text-center py-4 text-secondary small">
    &copy; <%= new Date().getFullYear() %> Tyre Inspector
  </footer>

  <script>
    // Auto-uppercase and trim spaces for the code input (now 8 chars)
    const codeEl = document.getElementById('code');
    if (codeEl) {
      codeEl.addEventListener('input', () => {
        codeEl.value = codeEl.value.replace(/\s+/g, '').toUpperCase().slice(0, 8);
      });
    }
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
